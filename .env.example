# Baked into the client at build time — set these in Railway → Variables
VITE_API_BASE_URL=https://your-backend.up.railway.app/api/

# Optional. If omitted, WebSocket URL is derived from VITE_API_BASE_URL
# VITE_WSS_BASE_URL=wss://your-backend.up.railway.app/
